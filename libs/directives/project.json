{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "directives",
  "prefix": "ngtw",
  "projectType": "library",
  "release": {
    "sourceRoot": "libs/directives/src",
    "version": {
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk",
      "manifestRootsToUpdate": ["dist/{projectRoot}"]
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "configurations": {
        "development": {
          "tsConfig": "libs/directives/tsconfig.lib.json"
        },
        "production": {
          "tsConfig": "libs/directives/tsconfig.lib.prod.json"
        }
      },
      "defaultConfiguration": "production",
      "executor": "@nx/angular:package",
      "metadata": {
        "description": "Builds the directives library for production or development."
      },
      "options": {
        "project": "libs/directives/ng-package.json"
      },
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"]
    },
    "build-library": {
      "configurations": {
        "development": {
          "args": ["--configuration=development", "--watch=true"],
          "parallel": true
        },
        "production": {
          "args": ["--configuration=production"],
          "parallel": false
        }
      },
      "defaultConfiguration": "production",
      "executor": "nx:run-commands",
      "metadata": {
        "description": "Builds the directives library for production."
      },
      "options": {
        "commands": ["nx run directives:tailwind", "nx run directives:build"]
      }
    },
    "tailwind": {
      "command": "npx @tailwindcss/cli -i {projectRoot}/_styles/input.css -o {projectRoot}/_styles/directives.css",
      "configurations": {
        "development": {
          "args": []
        },
        "production": {
          "args": ["--minify"]
        }
      },
      "defaultConfiguration": "production",
      "metadata": {
        "description": "Generates Tailwind CSS styles for the directives library."
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
